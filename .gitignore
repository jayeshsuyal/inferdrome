.DS_Store
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
*.egg-info/
build/
dist/

# The local demo stores evidence under ~/.inferdrome by default. Ignore an
# explicit in-repository workspace as a safety net.
.inferdrome-demo/
.inferdrome-compose/

# Opt-in real-GPU host state and generated proof runs are local evidence until
# a reviewed bundle is promoted deliberately.
.inferdrome-gpu/
gpu-proof-output/
gpu-proof-retrieved/

# Capability-spike runs are reviewed before selected artifacts are promoted to
# committed fixtures.
spikes/**/output/
